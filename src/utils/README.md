# Utils

Shared utility code is split into:

```text
core/        general utility functions
evaluation/  benchmark result parsing and aggregation
trainer/     trainer and evaluation helpers
```

Compatibility symlinks under `conformal/code` preserve legacy import paths.
