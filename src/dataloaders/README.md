# Dataloaders

`core/` contains the loader code moved from the previous
`conformal/code/loader` package.

The compatibility symlink at `conformal/code/loader` is kept so legacy imports
continue to work while datasets are standardized.
